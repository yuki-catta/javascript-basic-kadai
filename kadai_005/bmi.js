// 定数宣言
let bodyWeight;
let bodyLength;
let BMI;

// 身長・体重の変数に値を代入
bodyWeight = 68;
bodyLength = 1.7;

// BMIを計算
BMI = bodyWeight / (bodyLength * bodyLength);

// コンソールへの出力
console.log(BMI);